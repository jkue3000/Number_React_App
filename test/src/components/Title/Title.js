import React from "react";
import "./Title.css";

const Title = props => (
// Make the title for the game for the User to see
    <div className="titleplace">
        <h1>If You  Can Count You Can Win!</h1>
        <p>Don't Click the Same Number Twice</p>
    </div>

);
export default Title;